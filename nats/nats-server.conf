# NATS Server Configuration
port: 4222
http: 8222

jetstream {
  store_dir: "/data"
}

# Browser clients (nats.js wsconnect) — demo only; do not expose publicly without TLS/auth.
websocket {
  port: 9222
  no_tls: true
}
